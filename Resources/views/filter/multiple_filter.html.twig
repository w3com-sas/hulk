<div class="input-group mt-1">
    {% for filter in table.filters if filter.active == 'Y' %}
        {% if filter.type == 'multiple' %}
            <div class="input-group-prepend">
                <span class="input-group-text">{{ filter.label }}</span>
            </div>
            <input type="text" class="form-control multiple-filter" id="min{{ filter.field }}">
            <input type="text" class="form-control multiple-filter" id="max{{ filter.field }}">
        {% elseif filter.type == 'multiple-date' %}
            <div class="input-group-prepend">
                <span class="input-group-text">{{ filter.label }}</span>
            </div>
            <input type="date" class="form-control multiple-filter" id="min{{ filter.field }}">
            <input type="date" class="form-control multiple-filter" id="max{{ filter.field }}">
        {% endif %}
    {% endfor %}
</div>
