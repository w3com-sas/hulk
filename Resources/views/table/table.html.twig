<table class="table bg-white" id="dataTable">
    <thead>
    <tr role="row">
        {% for column in table.columns if column.active == 'Y' and column.hidden is null %}
            <th>{{ column.label }}</th>
        {% endfor %}
    </tr>
    </thead>
    <tbody>
    </tbody>
</table>