{% extends '@W3comHulk/display/base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col">
            <h2 class="mt-5 text-center text-gray">
                Tables mises à jour
            </h2>
            <div class="row my-5">
                <div class="col-md-6 mx-auto">
                    <div class="accordion" id="accordion">
                        {% if updatedEntities is empty %}
                            <div class="alert alert-info d-flex my-5">
                                <i class="fa fa-exclamation-triangle mr-3 my-auto"></i>
                                <p class="my-auto mb-0">
                                    aucune table n\'a été mis à jour.
                                </p>
                            </div>
                        {% else %}
                            {% for entity in updatedEntities %}
                                <div class="card">
                                    <div class="card-header d-flex justify-content-between" id="headingOne">
                                        <h2 class="mb-0">
                                            <button class="btn btn-link" type="button" data-toggle="collapse"
                                                    data-target="#collapse{{ entity.name }}">
                                                {{ entity.name }}
                                            </button>
                                        </h2>
                                        <button class="badge badge-primary badge-pill my-auto btn my-auto text-white"
                                                data-toggle="collapse" data-target="#collapse{{ entity.name }}">
                                            {{ entity.properties|length }} champ(s)
                                        </button>
                                    </div>

                                    <div id="collapse{{ entity.name }}" class="collapse" data-parent="#accordion">
                                        <div class="card-body">
                                            {% for property in entity.properties %}
                                                {{ property.field }} ,
                                            {% endfor %}
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        {% endif %}
                    </div>
                </div>
            </div>
            <h2 class="mt-5 text-center text-gray">
                Tables créées
            </h2>
            <div class="row my-5">
                <div class="col-md-6 mx-auto">
                    <div class="accordion" id="accordion">
                        {% if createdEntities is empty %}
                            <div class="alert alert-danger d-flex my-5">
                                <i class="fa fa-exclamation-triangle mr-3 my-auto"></i>
                                <p class="my-auto mb-0">
                                    aucune table n\'a été créée.
                                </p>
                            </div>
                        {% else %}
                            {% for entity in createdEntities %}
                                <div class="card">
                                    <div class="card-header d-flex justify-content-between" id="headingOne">
                                        <h2 class="mb-0">
                                            <button class="btn btn-link" type="button" data-toggle="collapse"
                                                    data-target="#collapse{{ entity.name }}">
                                                {{ entity.name }}
                                            </button>
                                        </h2>
                                        <button class="badge badge-primary badge-pill my-auto btn my-auto text-white"
                                                data-toggle="collapse" data-target="#collapse{{ entity.name }}">
                                            {{ entity.properties|length }} champ(s)
                                        </button>
                                    </div>

                                    <div id="collapse{{ entity.name }}" class="collapse" data-parent="#accordion">
                                        <div class="card-body">
                                            {% for property in entity.properties %}
                                                {{ property.field }} ,
                                            {% endfor %}
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        {% endif %}
                    </div>
                </div>
            </div>

            <a class="btn btn-block btn-success font-weight-bold mb-5"
               href="{{ path('w3com_display', { filename : filename }) }}">
                <i class="fa fa-arrow-circle-left float-left mt-1"></i>
                Display {{ filename }}
            </a>
            <a class="btn btn-block btn-secondary font-weight-bold mb-5"
               href="{{ path('w3com_display_admin', { filename : filename }) }}">
                <i class="fa fa-arrow-circle-right float-right mt-1"></i>
                Débogueur
            </a>

        </div>
    </div>
{% endblock %}