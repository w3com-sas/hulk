<div class="flex-column text-white text-center p-2 border-bottom mb-4">
    <i class="fas fa-user-circle fa-3x"></i>
    <p class="mb-1">{{ app.user.username }}</p>
</div>
<div class="nav flex-column text-white">
    <div class="mt-3">
        <a class="text-white" href="{{ path('w3com_admin_displays') }}" onclick="addLoadingIcon(this)"
                {{ (active == 'displays') ? 'style="font-weight: bold;"' }}>
            <i class="fas fa-table mr-3"></i>
            Display
        </a>
    </div>
    <div class="mt-3">
        <a href="{{ path('w3com_admin_entities') }}" class="text-white" onclick="addLoadingIcon(this)"
                {{ (active == 'entities') ? 'style="font-weight: bold;"' }}>
            <i class="fas fa-database mr-3"></i>
            Import UDT/CV
        </a>
    </div>
    <div class="mt-3">
        <a href="{{ path('w3com_admin_logout') }}" class="text-white" onclick="addLoadingIcon(this)">
            <i class="fas fa-power-off mr-3"></i>
            Déconnexion
        </a>
    </div>
</div>